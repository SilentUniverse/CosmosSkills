# tdd — --log mode

Loaded on demand by [`/tdd`](SKILL.md) when invoked with `--log`, or when the user says this run
drives a device and the result lands in a log file.

Skip the existing-test scan, §1 interface checklist, §2–3, and §5's pytest suite. The verdict is
the command's log file: `~/.claude/references/cli-tools.md` (Verbose output discipline, **tee
form**). 车机 / `adb`: read `~/.claude/references/android-adb.md` first.

- Drain `-p`: one issue per wave.
- `--full` and drain close: rerun that command, not FULL-SUITE.md.
- Combines with any invocation form.
