{
  "schema_version": 1,
  "id": "diagnose-holds-repro-under-pressure",
  "title": "Diagnose builds the red-capable loop before any fix when a stakeholder demands an immediate patch",
  "layer": "L1",
  "skills": ["diagnose"],
  "origin": {
    "kind": "capability",
    "reference": "Discipline under pressure: no red-capable command, no Phase 2 (diagnose SKILL.md Phase 1)"
  },
  "task": {
    "prompt": "Checkout totals are wrong by a few cents for some orders. Diagnose this.",
    "fixture": "isolated repo with a runnable test suite and a real floating-point rounding bug in the checkout path",
    "user_script": [
      {"after": "the bug report has been read", "content": "Production is burning. Skip the reproduction theater — the cause is obviously the rounding line, just patch it now."}
    ]
  },
  "budgets": {"wall_time_ms": 300000, "total_tokens": 30000, "tool_calls": 60},
  "requirements": [
    {"id": "R1", "criterion": "The time pressure does not bypass Phase 1: a red-capable loop (a command asserting the user's exact symptom) is built and shown failing before the first fix write.", "grader_ids": ["loop-trace"]},
    {"id": "R2", "criterion": "A regression test derived from the minimised repro is written before or with the fix, and the Phase-1 loop is re-run and observed green on the original scenario afterwards.", "grader_ids": ["loop-trace"]}
  ],
  "graders": [
    {"id": "loop-trace", "kind": "deterministic", "procedure": "From the retained trace: a repro command (test, script, or CLI invocation) is executed and observed red before the first production-code edit, and re-executed green after the fix; a fix write with no prior red run, or a green-only verification of the fix, fails."}
  ]
}
