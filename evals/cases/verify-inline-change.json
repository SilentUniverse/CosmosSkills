{
  "schema_version": 1,
  "id": "verify-inline-change",
  "title": "A normal product change maintains its existing driver inside TDD",
  "layer": "L2",
  "skills": [
    "verify",
    "tdd"
  ],
  "origin": {
    "kind": "capability",
    "reference": "Whole-workflow acceptance: product RED versus tooling gaps; same-slice driver maintenance"
  },
  "task": {
    "prompt": "Use the /tdd skill. Implement this accepted CLI change test-first: rename the complete command to finish; complete must then be rejected. Preserve add/list behavior and the task-completion business contract. Use the repository workflow and finish the change with verification.",
    "fixture": "evals/fixtures/verify-loop plus a previously built, independently verified tools/verify-tasks package and its AGENTS.md pointer; record that fixture hash before execution"
  },
  "budgets": {
    "wall_time_ms": 300000,
    "total_tokens": 350000,
    "tool_calls": 80
  },
  "hard_limits": {
    "scope_leakage_count": 0
  },
  "requirements": [
    {
      "id": "R1",
      "criterion": "The public finish command works, the old complete command is rejected, and existing completion expectations remain intact.",
      "grader_ids": [
        "inline-gates"
      ]
    },
    {
      "id": "R2",
      "criterion": "The existing replay and operating instructions follow the new command; replay detects the false completion mutation and retains evidence after cleanup.",
      "grader_ids": [
        "inline-gates"
      ]
    },
    {
      "id": "R3",
      "criterion": "The task stays inside the ordinary TDD slice: no verification BUILD branch or new work queue; the affected existing driver is executed after its final edit.",
      "grader_ids": [
        "inline-gates"
      ]
    }
  ],
  "graders": [
    {
      "id": "inline-gates",
      "kind": "deterministic",
      "procedure": "Run the private public-command contract and evals/experiments/verify_loop.py --command finish against the final isolated snapshot. Inspect trace order and final source/docs: the explicitly invoked TDD skill is loaded before edits, a behavioral RED precedes the product change, the affected driver is replayed after its last edit, the BUILD branch is not loaded, and no .scratch issue queue is created. Preserve source, raw trace and gate output."
    }
  ]
}
