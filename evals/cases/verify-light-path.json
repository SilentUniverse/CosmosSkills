{
  "schema_version": 1,
  "id": "verify-light-path",
  "title": "Existing checks bypass verification capability construction",
  "layer": "L1",
  "skills": [
    "verify",
    "tdd"
  ],
  "origin": {
    "kind": "routing",
    "reference": "Pstack adaptation comparison; executable fixture evals/fixtures/verify-loop"
  },
  "task": {
    "prompt": "Fix display_title so surrounding whitespace is removed while internal spaces are preserved. Add regression coverage and run the existing test command. Complete this small change.",
    "fixture": "evals/fixtures/verify-loop"
  },
  "budgets": {
    "wall_time_ms": 300000,
    "total_tokens": 180000,
    "tool_calls": 100
  },
  "hard_limits": {
    "scope_leakage_count": 0
  },
  "requirements": [
    {
      "id": "R1",
      "criterion": "The title contract passes independent tests and a regression test is retained.",
      "grader_ids": [
        "fixture-gates"
      ]
    },
    {
      "id": "R2",
      "criterion": "No verifier assets, workflow queue or meta-verification instructions are loaded or created.",
      "grader_ids": [
        "fixture-gates"
      ]
    }
  ],
  "graders": [
    {
      "id": "fixture-gates",
      "kind": "deterministic",
      "procedure": "Run evals/experiments/verify_loop.py outside the executor. Replay generated commands in isolated copies; inject a completion mutation; inspect public CLI audit, state cleanup, retained evidence, product hashes and cold-executor trace. For the light case use private boundary tests and trace/file checks. Retain per-stage gate.json."
    }
  ]
}
